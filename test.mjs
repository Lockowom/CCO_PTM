import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test() { const { error } = await supabase.from('tms_entregas').upsert({ nv: '999999', cliente: 'Test', estado: 'PENDIENTE' }, { onConflict: 'nv' }); console.log(error ? 'ERROR: ' + error.message : 'SUCCESS'); } test();